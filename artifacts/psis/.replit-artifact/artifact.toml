kind = "web"
previewPath = "/"
title = "PSIS - Pitch Sequence Intelligence"
version = "1.0.0"
id = "artifacts/psis"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22635

[services.development]
run = "pnpm --filter @workspace/psis run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/psis", "run", "build" ]
publicDir = "artifacts/psis/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22635"
BASE_PATH = "/"
