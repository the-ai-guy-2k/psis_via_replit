# Operator Screenshots (Optional)

This folder is reserved for operator documentation screenshots.

Suggested captures for future updates:

| File | Content |
|------|---------|
| `docker-pull.png` | Terminal showing successful `docker pull` |
| `container-running.png` | `docker ps` showing `psis` container Up |
| `homepage.png` | PSIS home page at http://localhost:8080 |
| `health-endpoint.png` | Browser showing `{"status":"ok"}` at `/api/healthz` |

Screenshots are optional. All procedures in this documentation are complete without images.
